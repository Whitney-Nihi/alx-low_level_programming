#include <stdio.h>

/**
 * main - Prints a text
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char text[200] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", text);
	return (0);
}
