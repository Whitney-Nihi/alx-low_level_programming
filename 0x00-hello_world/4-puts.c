#include <stdio.h>

/**
 * main - Prints a text
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char text[200] = "\"Programming is like building a multilingual puzzle\n";

	puts(text);
	return (0);
}
